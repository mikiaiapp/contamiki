version: '3'
services:
  contamiki:
    container_name: contamiki
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - /volume1/docker/contamiki/data:/app/data
