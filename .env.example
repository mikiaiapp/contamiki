# Renombra este archivo a .env y rellena los valores
# No uses comillas ni espacios extra

# Tu clave de API de Google Gemini (Obligatoria para que funcione la IA)
# Consíguela en: https://aistudio.google.com/app/apikey
API_KEY=tu_clave_api_aqui

# Una clave secreta para firmar las sesiones de usuario (Opcional, pero recomendada)
# Puede ser cualquier cadena de texto aleatoria
JWT_SECRET=cambia_esto_por_una_frase_secreta_segura